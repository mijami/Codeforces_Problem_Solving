#include <bits/stdc++.h>
 
using namespace std;
 
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
 
    int t;
 
    cin >> t;
 
    int n;
    long long int a,b,c;
    for(int i=0; i<t; i++)
    {
        cin >> a >> b >> c;
 
        long long int k = abs(a-b);
        long long int siz= k*2;
 
        if(k==1){
            cout << "-1" << "
";
 
        }
        else if(siz < c || siz <b || siz<a){
            cout << "-1" << "
";
 
        }
        else if(k>=c){
            k=k+c;
            cout << k << "
";
        }
 
        else{
            k=c-k;
            cout << k << "
";
        }
 
    }
 
 
    return 0;
}
 