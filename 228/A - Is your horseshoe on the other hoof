#include <bits/stdc++.h>
 
using namespace std;
 
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int a, b, c, d, n=0;
    cin >> a >> b >> c >>d;
    if(a==b || a== c || a==d){
        n++;
    }
 
    if( b== c || b==d){
        n++;
    }
    if( c==d){
        n++;
    }
    cout << n << "
";
    return 0;
 
}