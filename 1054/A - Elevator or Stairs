#include <bits/stdc++.h>
 
using namespace std;
#define checkmate return 0;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
 
    int x,y,z,a,b,c;
 
    cin >> x >> y >> z >> a >> b >> c;
 
    int walk=abs(x-y)*a;
    int ele = abs(x-y)*b+abs(x-z)*b+3*c;
    
    if(ele<=walk)
    {
        cout << "YES" << "
";
    }
    else {
        cout << "NO" << "
";
    }
 
    checkmate
}