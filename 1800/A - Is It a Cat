#include<bits/stdc++.h>
using namespace std;
using ll = long long int;
#define pb push_back
 
 
int main()
{
 
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t;
    cin >> t;
    while(t--)
    {
        int n;
        cin >> n;
        string a;
        cin >> a;
        int f=0;
        for(int i=0;i<n;i++)
        {
            if( a[i] != 'm' && a[i] != 'e' && a[i]!='o' && a[i]!='w'
               && a[i]!='M' && a[i]!='E' && a[i]!='O' && a[i]!='W' )
            {
                f=0;
                break;
            }
 
            if((a[i]=='m' || a[i]=='M') && f==0)
            {
                f++;
 
            }
 
            else if(f==0)
            {
                f=0;
                break;
            }
            if(f==1 && (a[i]=='m' || a[i]=='M'))
            {
                continue;
            }
            if( (a[i]=='e' || a[i]=='E') && f==1)
            {
                f++;
            }
            else if(f==1)
            {
                f=1;
                break;
            }
            if(f==2 && (a[i]=='e' || a[i]=='E'))
            {
                continue;
            }
            if((a[i]=='o' || a[i]=='O') && f==2)
            {
                f++;
            }
            else if(f==2)
            {
                f=2;
                break;
            }
            if((a[i]=='o' || a[i]=='O') && f==3)
            {
                continue;
            }
            if((a[i]=='w' || a[i]=='W') && f==3)
            {
                f++;
            }
            else if(f==3)
            {
                f=3;
                break;
            }
            if(f==4 && (a[i]=='w' || a[i]=='W') )
            {
                continue;
            }
            else if (f==4){
                f=0;
                break;
            }
 
 
        }
        if(f>=4) cout << "YES
";
        else cout << "NO
";
 
    }
   return 0;
}